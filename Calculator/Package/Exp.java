package Package;

class Exp extends ScientificFunction {
   
    double exp(){
        System.out.print("Enter the value of x: ");
        x = input.nextDouble();
        return result = Math.exp(x);
        
    }

}
