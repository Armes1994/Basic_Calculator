package Package;
class Sine extends ScientificFunction {  
    double sine(){
        System.out.print("Enter the value of x: ");
        x = input.nextDouble();
        return result = Math.sin(x);
    }
}
