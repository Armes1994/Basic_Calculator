package Package;
class Cosine extends ScientificFunction {
    double cosine(){
        System.out.print("Enter the value of x: ");
        x = input.nextDouble();
        return result = Math.cos(x);
    }
}
