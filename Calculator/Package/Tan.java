package Package;
class Tan extends ScientificFunction {
    double tan(){
        System.out.print("Enter the value of x: ");
        x = input.nextDouble();
        return result = Math.tan(x);
    }
}
